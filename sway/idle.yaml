---
debug: false
wait: true
timeouts:
  - timeout: 300
    command: '~/.config/sway/lock --daemonize'
  - timeout: 600
    command: 'swaymsg "output * dpms off"'
    resume: 'swaymsg "output * dpms on"'
before-sleep: '~/.config/sway/lock --daemonize'
lock: '~/.config/sway/lock --daemonize'
unlock: 'sh -c "pkill -u $(id -u) -9 swaylock"'
